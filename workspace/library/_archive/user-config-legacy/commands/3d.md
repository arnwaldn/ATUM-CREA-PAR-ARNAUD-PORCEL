---
description: 3D development (Blender, Unity, Three.js) (user)
---

# /3d - 3D Development

## USAGE
```
/3d blender "Create low-poly tree asset"
/3d unity "Setup 3D character controller"
/3d threejs "3D product viewer"
/3d export "blender-scene.blend" --format=gltf
```

## MODES

### blender
Scripts Python pour Blender
```
/3d blender "Procedural terrain generation"
```
Output: Script Python Blender

### unity
Scripts C# pour Unity 3D
```
/3d unity "Third-person camera controller"
```
Output: Scripts C# Unity

### threejs
Scenes Three.js web
```
/3d threejs "Interactive 3D model viewer"
```
Output: Code Three.js + React

### export
Conversion/export assets
```
/3d export "model.blend" --format=gltf
```

## BLENDER SCRIPTS

### Structure type
```python
import bpy
import bmesh

def create_asset():
    # Clear existing
    bpy.ops.object.select_all(action='SELECT')
    bpy.ops.object.delete()

    # Create mesh
    mesh = bpy.data.meshes.new("AssetMesh")
    obj = bpy.data.objects.new("Asset", mesh)

    # Link to scene
    bpy.context.collection.objects.link(obj)

    # Create geometry with bmesh
    bm = bmesh.new()
    # ... geometry creation
    bm.to_mesh(mesh)
    bm.free()

    return obj

if __name__ == "__main__":
    create_asset()
```

### Assets types
- Low-poly models
- Procedural generation
- UV unwrapping
- Material setup
- Animation rigs

## THREE.JS STRUCTURE

```tsx
// components/ModelViewer.tsx
import { Canvas } from '@react-three/fiber'
import { OrbitControls, useGLTF } from '@react-three/drei'

function Model({ url }: { url: string }) {
  const { scene } = useGLTF(url)
  return <primitive object={scene} />
}

export function ModelViewer({ modelUrl }: Props) {
  return (
    <Canvas camera={{ position: [0, 0, 5] }}>
      <ambientLight intensity={0.5} />
      <directionalLight position={[10, 10, 5]} />
      <Model url={modelUrl} />
      <OrbitControls />
    </Canvas>
  )
}
```

## UNITY SCRIPTS

```csharp
using UnityEngine;

public class ThirdPersonController : MonoBehaviour
{
    [SerializeField] private float moveSpeed = 5f;
    [SerializeField] private float rotationSpeed = 10f;

    private CharacterController controller;
    private Vector3 moveDirection;

    void Start()
    {
        controller = GetComponent<CharacterController>();
    }

    void Update()
    {
        float horizontal = Input.GetAxis("Horizontal");
        float vertical = Input.GetAxis("Vertical");

        moveDirection = new Vector3(horizontal, 0, vertical);
        moveDirection = transform.TransformDirection(moveDirection);

        controller.Move(moveDirection * moveSpeed * Time.deltaTime);
    }
}
```

## OPTIONS
| Option | Description |
|--------|-------------|
| --format=X | Export format (gltf, fbx, obj) |
| --lowpoly | Optimiser pour game |
| --textures | Inclure textures |
| --animated | Inclure animations |

## FORMATS EXPORT
| Format | Usage |
|--------|-------|
| GLTF/GLB | Web, Three.js |
| FBX | Unity, Unreal |
| OBJ | Universal |
| USD | Pipeline pro |

## MCP UTILISES
- Context7 (docs Three.js, Unity)
- filesystem (scripts)
- Blender MCP (si disponible)
